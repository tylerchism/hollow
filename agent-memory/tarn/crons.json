[
  {
    "name": "morning_brief",
    "schedule": "0 6 * * *",
    "tz": "America/Chicago",
    "prompt": "Good morning. Run the morning brief for Tyler. Structure it in this order:\n\n(1) 🤖 AI NEWS — Use WebSearch to find the top 3-5 AI developments from the last 24 hours. Focus on model releases, significant research, and anything relevant to multi-agent systems or Claude/Anthropic specifically. One sentence per item with a link.\n\n(2) 🎪 UPCOMING CONFERENCES — Use xsearch and WebSearch to find conferences and events in the next 6 months that match Tyler's interests: regenerative agriculture, homesteading, permaculture, ancestral health, Weston A. Price, food is medicine, mitochondrial health, rewilding, Kyle Cease events, libertarian/America First gatherings, food freedom, rogue food. Only include events Tyler likely hasn't heard of or hasn't been reminded of recently — skip FreedomFest (already booked). Include: event name, date, location, and registration link if available. Aim for 3-5 events, prioritize ones with near registration deadlines. After finding conferences, post just the conferences section to #events-conferences using: `send_discord_channel events-conferences \"message\"`\n\n(3) 💡 GOAL-ALIGNED IDEAS — Generate 2 fresh ideas that could push Tyler meaningfully toward his goals. Do not pull from the MC list — these should be original, specific, and actionable. Goals to draw from: land sovereignty / homesteading, ancestral health, growing Hollow, community connection. One sentence each — seed, not essay. After generating, cross-post just the ideas section to #ideas using: `send_discord_channel ideas \"message\"`\n\n(4) 💛 SOCIAL / DATING — One fresh idea or upcoming local event for meeting women who share Tyler's values. Tyler is single, introverted, based in Calico Rock AR, frequently in NW Arkansas (Fayetteville/Bentonville). Best environments: shared-interest settings where the activity does the talking — foraging walks, homestead skill shares, farmers markets, fermentation workshops, farm dinners, regen ag events, ancestral health gatherings, Kyle Cease events, permaculture convergences. Use WebSearch or xsearch to find a real local or regional event if possible; otherwise generate a specific creative idea. One item, concrete and actionable.\n\n(5) 🛠 WHAT THE TEAM CAN DO FOR TYLER — Generate 2-3 specific things the Hollow team could do autonomously this week to advance Tyler's mission and goals. These are NOT tasks for Tyler to do, and NOT team performance upgrades — they are things Tarn and the specialists can execute on Tyler's behalf. Examples: research a specific event and pull together registration details, find and summarize a key resource on a goal Tyler is pursuing, identify a local community or person worth connecting with, draft an outreach message, scout land listings in the Ozarks matching Tyler's criteria, pull together intel on a topic Tyler cares about. Draw from: land sovereignty / homesteading, ancestral health, community connection, meeting aligned people, growing Hollow's capabilities for Tyler's life. Be specific — not \"research homesteading\" but \"find and summarize the top 3 permaculture land co-op models operating in Arkansas or Missouri right now.\"\n\nKeep each section tight. Return the full brief as your final response — the system automatically posts it to #morning-brief. Do NOT call send_msg or send_discord (those go to #tarn). If you want to send an interim status update during the run, use: send_discord_channel morning-brief \"Starting morning brief\"",
    "model": "claude-sonnet-4-6",
    "is_main_session": true,
    "notify_telegram": false,
    "discord_channel_name": "morning-brief",
    "chat_id": "cron_morning_brief",
    "enabled": false
  },
  {
    "name": "ideas_review",
    "schedule": "10 */3 * * *",
    "tz": "America/Chicago",
    "prompt": "Run Ideas Review. NOTE: handle this yourself without consulting other agents. Steps: 1. Run `mc ideas list` to find ideas with status=ready. If none, stop and say so. 2. For each ready idea, review the title and body yourself. 3. Evaluate strategic value, identify risks/scope traps, and structure as an actionable ticket. 4. Create the task via `mc tasks create` with a synthesized description including acceptance criteria and subtasks. 5. PATCH the idea to status=converted and set linked_task_id to the new task id. 6. Return your summary as the final response — the system automatically posts it to #ideas. Do NOT call send_msg or send_discord (those go to #tarn).",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "ideas",
    "chat_id": "cron_ideas_review",
    "enabled": false,
    "gate_command": "python3 -c \"import json,subprocess; r=subprocess.run(['curl','-s','http://localhost:3333/api/ideas?status=ready','-H','x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'],capture_output=True,text=True); ideas=json.loads(r.stdout) if r.stdout.strip() else []; exit(0 if ideas else 1)\""
  },
  {
    "name": "backlog_triage",
    "schedule": "30 */4 * * *",
    "tz": "America/Chicago",
    "prompt": "Run Backlog Triage. NOTE: handle this yourself without consulting other agents. (1) Run `mc tasks list --status=backlog` to collect backlog tasks. (2) For each task: is it actionable and scoped? mark ready. Needs Tyler? mark blocked with blocked_reason + needs-tyler tag. Too vague? leave in backlog with note. (3) Execute mc tasks update calls based on your decisions. (4) DEPRIORITIZE STALE BLOCKED TASKS — Run `mc tasks list --status=blocked`. For each blocked task, compute age using: `date -d \"[created_at]\" +%s` vs `date +%s`. Any task blocked for 96h or more that has not been touched: run `mc tasks update <id> --status=deprioritized`. Return your triage summary as the final response — the system automatically posts it to #tasks. Do NOT call send_msg or send_discord (those go to #tarn).",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "tasks",
    "chat_id": "cron_backlog_triage",
    "enabled": false,
    "gate_command": "python3 -c \"import json,subprocess; r=subprocess.run(['curl','-s','http://localhost:3333/api/tasks?status=backlog','-H','x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'],capture_output=True,text=True); tasks=json.loads(r.stdout) if r.stdout.strip() else []; exit(0 if tasks else 1)\""
  },
  {
    "name": "task_executor",
    "schedule": "0 9,13,17,21 * * *",
    "tz": "America/Chicago",
    "prompt": "Run the Task Executor. Run `mc tasks list --status=ready` and find all ready tasks sorted by priority (high first). For each ready task:\n\n(0) NEEDS-REVIEW SAFETY CHECK — Before doing anything else, check the task's `tags` array for the string \"needs-review\". If present, this task was reset by the watchdog after an apparent crash and must be safety-checked by Briar before proceeding.\n\nIf \"needs-review\" tag IS present:\n  (0a) Review the task yourself: check if it's filesystem/git-touching based on its title and description. If it looks risky (e.g. file writes, git operations, external API calls that could duplicate), PATCH task to blocked status, set blocked_reason to your concern, add \"needs-tyler\" tag, and post to Discord: `send_discord_channel tasks \"⚠️ Crashed task [title] flagged as potentially unsafe to auto-retry. Needs Tyler review.\"` Skip to next task — do NOT proceed with execution.\n  (0b) If the task appears safe to re-run: remove the needs-review tag via PATCH (rebuild tags array without \"needs-review\"), then continue to step (1) as normal.\n  (0c) ALWAYS remove the needs-review tag after the check regardless of outcome (it must never be left on the task). Use:\n    curl -s -X PATCH http://localhost:3333/api/tasks/<id> -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d '{\"tags\": [<tags array without needs-review>]}'\n\nIf \"needs-review\" tag is NOT present: proceed directly to step (1).\n\n(1) CLAIM WITH IMMEDIATE HEARTBEAT (atomic — eliminates null-heartbeat window): NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); LEASE=$(date -u -d '+120 minutes' +%Y-%m-%dT%H:%M:%SZ); curl -s -X PATCH http://localhost:3333/api/tasks/<id> -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d \"{\\\"status\\\": \\\"in_progress\\\", \\\"heartbeat_at\\\": \\\"$NOW\\\", \\\"lease_expires_at\\\": \\\"$LEASE\\\", \\\"locked_by\\\": \\\"task_executor\\\"}\" Do NOT call `mc tasks update --status=in_progress` first — that creates a null-heartbeat window where the watchdog is blind. This single curl does both atomically. (1c) REAL-TIME ROUTE LOG — immediately after determining the route (before executing), write a routing.decision entry: curl -s -X POST http://localhost:3333/api/agent-activity -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d '{\"agent\": \"tarn\", \"action\": \"routing.decision\", \"action_type\": \"routing.decision\", \"details\": \"routing [task_title] → [route_chosen]\", \"event_type\": \"routing.decision\", \"route_chosen\": \"[route_chosen]\", \"summary\": \"routing [task_title] to [route_chosen]\"}'. This must happen at the moment you decide the route — NOT after the task is completed. (2) Route based on task type: coding/files/execution → use Bash to spawn Claude Code (see HEARTBEAT section below); research/analysis → handle directly yourself; quick/coordination → handle directly. ISOLATION CHECK: before spawning Claude Code, inspect the task's `isolation` field (returned in the task API response). If `isolation == \"worktree\"`, pass `isolation: \"worktree\"` as a parameter to the Agent tool when spawning Claude Code — this triggers EnterWorktree at start and ExitWorktree at end, keeping changes isolated until explicitly merged. If `isolation` is null or absent, spawn Claude Code normally. (3) Complete the task. (4) CRITICAL — SPIKE CLOSING PROTOCOL: if the task title starts with 'SPIKE:' or is tagged 'spike', apply the 3-state closing protocol before marking done. First, fetch the full task via `mc tasks list` to read its `next_task` field.\n\nSTATE A — CONVERT (spike had a clear next step, next_task field is populated): Auto-create the downstream task: `mc tasks create \"[next_task.title]\" --description=\"[next_task.description]\" --priority=[next_task.priority] --tags=\"[next_task.tags]\" --status=ready`. Log: 'SPIKE CONVERT: auto-queued downstream task [new_task_id] for Tyler approval'. Note: newly created tasks default to backlog but the intent is Tyler reviews and promotes to ready. If `next_task.tags` includes 'needs-tyler', create with `--status=blocked --blocked-reason=\"Needs Tyler decision before work can begin\"`.\n\nSTATE B — ESCALATE (spike result requires a Tyler decision before work can proceed, next_task is absent but conclusion is inconclusive or multi-path): Create a decision task: `mc tasks create \"DECISION: [short decision title]\" --description=\"[what must be decided, options, spike findings]\" --tags=\"needs-tyler\" --status=blocked --blocked-reason=\"SPIKE escalation: awaiting Tyler direction\"`. Do NOT auto-queue build work. Log: 'SPIKE ESCALATE: routed to pending-decisions as [task_id]'.\n\nSTATE C — CLOSE WITHOUT CONVERSION (spike was conclusive and negative, or no build is needed): Mark done with a notes field explaining why no downstream task was created. Log: 'SPIKE CLOSE: no conversion — [reason in one sentence]'.\n\nIF `next_task` is absent AND the spike's findings clearly warrant follow-on build work (State A situation with missing data), warn Tyler before marking done: `send_discord_channel tasks \"⚠ SPIKE [task title] completed without next_task populated. Downstream task was NOT auto-queued — manual follow-up required.\"`\n\nThe spike is not done until one of the three states has been executed. (5) `mc tasks update <id> --status=done`. (6) Write a completion entry to the activity log using the structured curl format (do NOT use `mc activity log` — it does not reliably pass metadata fields): curl -s -X POST http://localhost:3333/api/agent-activity -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d '{\"agent\": \"tarn\", \"action\": \"task.completed\", \"action_type\": \"task.completed\", \"details\": \"completed: [task title]\", \"event_type\": \"task.completed\", \"route_chosen\": \"[route_chosen]\", \"summary\": \"completed [task title] via [route_chosen]\"}'. Process ALL ready tasks, not just one. Never mark done without actually completing the work. Return your execution summary as the final response — the system automatically posts it to #tasks. Do NOT call send_msg or send_discord (those go to #tarn).\n\n\nLONG-RUNNING TASK PATTERN: If a task is flagged with `long_running: true` in its description or tags, or if it's expected to take >5 minutes, use the fire+wait-job pattern instead of synchronous hail:\n  1. Fire:  JOB_ID=$(hail --detach <agent> '<task description>')\n  2. Store: mc tasks update <id> --notes='fire_job_id: '$JOB_ID\n  3. Wait with heartbeat: bin/wait-job <agent> $JOB_ID --task-id <mc_task_id>\n  4. On exit 0: continue to mark done. On exit 1: mark blocked with error. On exit 2 (cancelled/404): re-fire the job. On exit 3 (timeout): mark blocked as timed out.\n\nRECOVERY: If a task is in_progress and its notes field contains 'fire_job_id:', extract the job_id and poll /result/<job_id> first. If status=pending, resume wait-job. If status=404 (agent restarted), re-fire.\nHEARTBEAT — REQUIRED when spawning Claude Code via Bash for a task:\nBefore spawning Claude Code, set the lease so the watchdog knows this task is actively running:\n  NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ)\n  LEASE=$(date -u -d '+120 minutes' +%Y-%m-%dT%H:%M:%SZ)\n  curl -s -X PATCH http://localhost:3333/api/tasks/<id> -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d \"{\\\"heartbeat_at\\\": \\\"$NOW\\\", \\\"lease_expires_at\\\": \\\"$LEASE\\\", \\\"locked_by\\\": \\\"task_executor\\\"}\"\nThen start a background heartbeat loop BEFORE launching Claude Code (run this in the background with & ):\n  while true; do sleep 900; NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ); LEASE=$(date -u -d '+120 minutes' +%Y-%m-%dT%H:%M:%SZ); curl -s -X PATCH http://localhost:3333/api/tasks/<id> -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d \"{\\\"heartbeat_at\\\": \\\"$NOW\\\", \\\"lease_expires_at\\\": \\\"$LEASE\\\"}\" > /dev/null 2>&1; done &\n  HEARTBEAT_PID=$!\nAfter Claude Code finishes, kill the heartbeat and clear the lease:\n  kill $HEARTBEAT_PID 2>/dev/null\n  curl -s -X PATCH http://localhost:3333/api/tasks/<id> -H 'Content-Type: application/json' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -d '{\"heartbeat_at\": null, \"lease_expires_at\": null, \"locked_by\": null}'\nFor hail-routed tasks (not Claude Code), use hail_lease_minutes=45 instead of 120.\n\nCONTEXT RECITATION — REQUIRED at every hail or Claude Code spawn:\nBefore each hail call or Claude Code subprocess spawn, construct a <task_context> block containing:\n- Task title and MC task ID\n- Current step number and name (e.g., \"Step 2 of 5: clip extraction\")\n- List of completed steps with outcomes (e.g., \"Step 1: corpus search — 47 candidates found\")\n- Remaining steps with brief descriptions\n- Path to progress.json if the task uses checkpoint files\n- Path to plan file if the task has one\nPass this block via hail --context or as preamble in Claude Code prompts.\nThis prevents sub-agents from losing track of where they are in a multi-step task after restarts or long-running operations.\n\nPARALLEL DISPATCH — use when a plan file has independent subtasks:\nWhen a task has a plan file (plan.md or plan.json) with multiple independent subtasks at the same autonomy level and with no inter-dependencies, dispatch them in parallel using --detach + --wait-all instead of running them sequentially.\nPattern:\n  1. Identify independent subtasks from the plan (no data/file dependencies between them).\n  2. Fire each in parallel:\n       JOB1=$(hail --detach <agent1> '<subtask1>' --context '<task_context>')\n       JOB2=$(hail --detach <agent2> '<subtask2>' --context '<task_context>')\n  3. Wait for all to finish:\n       hail --wait-all $JOB1:<agent1> $JOB2:<agent2>\n  4. Collect combined results and continue with dependent steps.\nOnly use parallel dispatch when tasks are truly independent (no shared mutable state, no sequential ordering required). Always maintain heartbeats during the wait (see HEARTBEAT section above). If any parallel job fails, hail --wait-all exits 1 — mark the parent task blocked with the error details.",
    "model": "claude-sonnet-4-6",
    "is_main_session": true,
    "notify_telegram": false,
    "discord_channel_name": "tasks",
    "chat_id": "cron_task_executor",
    "enabled": false
  },
  {
    "name": "team_retro",
    "schedule": "0 18 * * *",
    "tz": "America/Chicago",
    "prompt": "Run Team Retro. NOTE: handle this yourself without consulting other agents. (1) Run `mc tasks list` to collect done/blocked/in_progress tasks. (2) TYLER DECISION THROUGHPUT — compute: (a) Count tasks moved blocked→done in last 48h. (b) List all currently blocked tasks with blocked_reason, sorted by age. (c) If count in (a) is zero, lead with '⚠ ZERO TYLER DECISIONS IN LAST 48H' and list pending decisions by age. (3) Review the data yourself: identify patterns, process improvements, concerns, and failure modes. (4) ROUTING QUALITY AUDIT — Run `mc activity list --type=routing.decision --limit=20` to fetch the last 20 routing decisions. Audit for: tasks Tarn self-handled that should have gone to a specialist, wrong routing choices, patterns of over/under-delegation. (5) If you identify process changes needed, create a task or idea via mc CLI. (6) MANDATORY CLOSING — for every stale block, routing violation, or actionable improvement: run `mc tasks create \"[short title]\" --description=\"[finding detail]\" --priority=medium --assigned-to=tarn --status=ready`. At least one task must be created per actionable finding or explicitly state: 'no actionable findings this cycle.' (7) Return your retro summary as the final response — the system automatically posts it to #tasks. Tyler decision throughput metric always appears first. Do NOT call send_msg or send_discord (those go to #tarn).",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "tasks",
    "chat_id": "cron_team_retro",
    "enabled": false
  },
  {
    "name": "ai_money_ideas",
    "schedule": "0 14 * * *",
    "tz": "America/Chicago",
    "prompt": "Generate fresh AI money-making ideas for Tyler. Tyler's context: builder, runs a multi-agent AI system, wants to fund regenerative agriculture and permaculture land purchases. He is tech-savvy and can build things.\n\nSteps:\n1. Use WebSearch and xsearch to research: current AI monetization trends, new AI tools people are building income around, trading bot strategies, AI automation services, prompt engineering niches, AI agent services, content monetization with AI, etc. Cast a wide net.\n2. Brainstorm with the research — generate 5-7 fresh, specific, actionable ideas. Mix of:\n   - Quick wins (can start this week)\n   - Medium-term builds (weeks to months)\n   - High-ceiling plays (trading bots, SaaS, agencies)\n3. For each idea, post it as a SEPARATE message to #ai-money using one send_discord_channel call per idea. Format each standalone message as:\n\n**[Idea Name]**\n[2-3 sentence description: what it is, how to start, rough income potential]\nDifficulty: Easy/Medium/Hard\n\nCall send_discord_channel once per idea, like:\n  send_discord_channel ai-money \"**Idea Name**\\n[description]\\nDifficulty: Medium\"\n\nDo NOT bundle all ideas into a single message. Each idea must be its own separate message so users can react to them individually.\n\n4. Return a brief summary (e.g. \"Posted 7 ideas to #ai-money\") as your final response. Do NOT include the ideas list in the final response — they are already posted.\n\nDo NOT call send_msg or send_discord (those go to #tarn). Only use send_discord_channel.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_ai_money_ideas",
    "enabled": false
  },
  {
    "name": "watchdog",
    "schedule": "*/30 * * * *",
    "tz": "America/Chicago",
    "prompt": "Run the Task Watchdog. SQL-only, no LLM calls. Steps:\n\n(1) HEALTH CHECK — abort if MC is unhealthy:\n  STATUS=$(curl -s http://localhost:3333/api/health | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status','unknown'))\" 2>/dev/null)\n  If STATUS is not 'ok' or 'degraded', print 'MC unhealthy — watchdog aborting' and exit.\n\n(2) NULL-HEARTBEAT PHANTOM DETECTION — find in_progress tasks where heartbeat_at IS NULL AND updated_at > 15 minutes ago. These are phantom-stuck tasks: the executor claimed them (set status=in_progress) but died before writing any heartbeat.\n\npython3 << 'NHEOF'\nimport json, subprocess, sys\nfrom datetime import datetime, timezone, timedelta\n\napi_key = '462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\nbase = 'http://localhost:3333/api'\nNULL_HB_THRESHOLD_SECONDS = 15 * 60  # 15 minutes\nMAX_RESETS = 3  # after 3 resets, flip to condition_blocked to prevent thrash loop\n\ndef api_get(path):\n    r = subprocess.run(['curl', '-s', f'{base}{path}', '-H', f'x-api-key: {api_key}'], capture_output=True, text=True)\n    try: return json.loads(r.stdout)\n    except: return None\n\ndef api_patch(path, data):\n    r = subprocess.run(['curl', '-s', '-X', 'PATCH', f'{base}{path}', '-H', f'x-api-key: {api_key}', '-H', 'Content-Type: application/json', '-d', json.dumps(data)], capture_output=True, text=True)\n    return r.returncode == 0\n\nip_tasks = api_get('/tasks?status=in_progress') or []\nif not isinstance(ip_tasks, list):\n    print('null-hb: failed to fetch in_progress tasks')\n    sys.exit(0)\n\nnow = datetime.now(timezone.utc)\nphantom_count = 0\nceiling_count = 0\n\nfor task in ip_tasks:\n    tid = task.get('id')\n    title = task.get('title', '')\n    heartbeat_at = task.get('heartbeat_at')\n    updated_at = task.get('updated_at')\n    attempt_count = task.get('attempt_count', 0)\n    stale_count = task.get('stale_count', 0)\n    tags = task.get('tags') or []\n\n    # Only process tasks with NULL heartbeat\n    if heartbeat_at is not None:\n        continue\n\n    if not updated_at:\n        continue\n\n    try:\n        updated_dt = datetime.fromisoformat(updated_at.replace('Z', '+00:00'))\n    except Exception:\n        continue\n\n    age_seconds = (now - updated_dt).total_seconds()\n    if age_seconds <= NULL_HB_THRESHOLD_SECONDS:\n        continue  # Too recent — still starting up, give it time\n\n    # Phantom-stuck task detected. Check reset_count (use stale_count as proxy).\n    # reset_count is tracked via the 'watchdog-reset-count' tag value or stale_count.\n    # Use attempt_count to track number of watchdog resets (consistent with expired-lease logic).\n    if attempt_count >= MAX_RESETS:\n        # Ceiling hit — flip to condition_blocked to prevent thrash loop\n        new_tags = list(set(tags + ['condition-blocked', 'watchdog-ceiling']))\n        new_tags = [t for t in new_tags if t not in ('needs-review',)]\n        ok = api_patch(f'/tasks/{tid}', {\n            'status': 'condition_blocked',\n            'blocked_reason': f'Watchdog: null-heartbeat phantom detected {attempt_count} times — ceiling hit ({MAX_RESETS} resets). Manual review required.',\n            'tags': new_tags,\n            'heartbeat_at': None,\n            'lease_expires_at': None,\n            'locked_by': None\n        })\n        if ok:\n            print(f'[CEILING] Task {tid} ({title[:50]}): null-hb phantom, reset_count={attempt_count} >= {MAX_RESETS} — flipped to condition_blocked')\n            ceiling_count += 1\n    else:\n        # Reset to ready with needs-review tag\n        new_tags = list(set(tags + ['needs-review']))\n        ok = api_patch(f'/tasks/{tid}', {\n            'status': 'ready',\n            'attempt_count': attempt_count + 1,\n            'stale_count': stale_count + 1,\n            'tags': new_tags,\n            'heartbeat_at': None,\n            'lease_expires_at': None,\n            'locked_by': None\n        })\n        if ok:\n            print(f'[NULL-HB] Task {tid} ({title[:50]}): null-heartbeat phantom reset to ready (reset {attempt_count+1}/{MAX_RESETS}), tagged needs-review')\n            phantom_count += 1\n\nif phantom_count == 0 and ceiling_count == 0:\n    print('null-hb: no null-heartbeat phantom tasks found')\nelse:\n    print(f'null-hb: {phantom_count} phantom task(s) reset to ready, {ceiling_count} hit ceiling and flipped to condition_blocked')\nNHEOF\n\n\nIMPORTANT — RESPECT expected_duration_minutes: When checking lease expiry in step (3), respect the task's `expected_duration_minutes` field if present. If `expected_duration_minutes` is set and the task's heartbeat_at is more recent than `expected_duration_minutes` minutes ago, do NOT flag it as expired — the task declared it needs more time. Only flag tasks where the lease has expired AND the elapsed time since heartbeat_at exceeds `expected_duration_minutes` (or 15 minutes if not set). In the python3 block for step (4), add this check before processing each expired task:\n  expected_mins = task.get('expected_duration_minutes') or 15\n  heartbeat_at = task.get('heartbeat_at')\n  if heartbeat_at:\n      try:\n          hb_dt = datetime.fromisoformat(heartbeat_at.replace('Z', '+00:00'))\n          elapsed_mins = (now - hb_dt).total_seconds() / 60\n          if elapsed_mins < expected_mins:\n              print(f'[SKIP] Task {tid}: lease expired but within expected_duration_minutes={expected_mins} (elapsed={elapsed_mins:.1f}m) — skipping')\n              continue\n      except Exception:\n          pass\n\n(3) FIND EXPIRED LEASES — find all in_progress tasks where lease_expires_at is set and has passed:\n  NOW=$(date -u +%Y-%m-%dT%H:%M:%SZ)\n  EXPIRED=$(curl -s 'http://localhost:3333/api/tasks?status=in_progress' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' | python3 -c \"\nimport json, sys\nfrom datetime import datetime, timezone\ntasks = json.load(sys.stdin)\nnow = datetime.now(timezone.utc)\nexpired = [t for t in tasks if t.get('lease_expires_at') and datetime.fromisoformat(t['lease_expires_at'].replace('Z','+00:00')) < now]\nprint(json.dumps(expired))\n\")\n\n(4) FOR EACH EXPIRED TASK — process based on attempt_count vs max_attempt_count (3):\n  Use python3 to iterate and call PATCH for each expired task:\n\n  python3 << 'EOF'\nimport json, subprocess, sys\nfrom datetime import datetime, timezone\n\nexpired = json.loads('''EXPIRED_JSON_HERE''')  # replace with actual $EXPIRED value\nmax_attempts = 3\napi_key = '462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\n\nfor task in expired:\n    tid = task['id']\n    title = task['title']\n    attempt_count = task.get('attempt_count', 0)\n    stale_count = task.get('stale_count', 0)\n    tags = task.get('tags', [])\n\n    if attempt_count < max_attempts:\n        # Reset to ready, increment counters, tag needs-review\n        new_tags = list(set(tags + ['needs-review']))\n        payload = json.dumps({\n            'status': 'ready',\n            'attempt_count': attempt_count + 1,\n            'stale_count': stale_count + 1,\n            'tags': new_tags,\n            'heartbeat_at': None,\n            'lease_expires_at': None,\n            'locked_by': None\n        })\n        result = subprocess.run(['curl', '-s', '-X', 'PATCH', f'http://localhost:3333/api/tasks/{tid}',\n            '-H', 'Content-Type: application/json', '-H', f'x-api-key: {api_key}', '-d', payload],\n            capture_output=True, text=True)\n        print(f'[WARN] Task {tid} ({title[:50]}): lease expired, attempt {attempt_count+1}/{max_attempts} — reset to ready, tagged needs-review')\n    else:\n        # Max retries reached — mark failed\n        new_tags = list(set(tags + ['max-retries-reached']))\n        payload = json.dumps({\n            'status': 'blocked',\n            'blocked_reason': f'Watchdog: max retries reached ({attempt_count} attempts). Task failed to complete within lease window.',\n            'tags': new_tags,\n            'heartbeat_at': None,\n            'lease_expires_at': None,\n            'locked_by': None\n        })\n        result = subprocess.run(['curl', '-s', '-X', 'PATCH', f'http://localhost:3333/api/tasks/{tid}',\n            '-H', 'Content-Type: application/json', '-H', f'x-api-key: {api_key}', '-d', payload],\n            capture_output=True, text=True)\n        print(f'[ERROR] Task {tid} ({title[:50]}): max retries reached ({attempt_count} attempts) — marked blocked with max-retries-reached')\nEOF\n\n(5) STALE READY CHECK — find tasks stuck in ready status for more than 4 hours (indicates task_executor missed them):\n\npython3 << 'PYEOF'\nimport json, subprocess, sys\nfrom datetime import datetime, timezone\n\napi_key = '462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\nbase = 'http://localhost:3333/api'\nSTALE_THRESHOLD_SECONDS = 4 * 3600  # 4 hours\n\ndef api_get(path):\n    r = subprocess.run(['curl', '-s', f'{base}{path}', '-H', f'x-api-key: {api_key}'], capture_output=True, text=True)\n    try: return json.loads(r.stdout)\n    except: return None\n\ndef api_patch(path, data):\n    r = subprocess.run(['curl', '-s', '-X', 'PATCH', f'{base}{path}', '-H', f'x-api-key: {api_key}', '-H', 'Content-Type: application/json', '-d', json.dumps(data)], capture_output=True, text=True)\n    return r.returncode == 0\n\nready_tasks = api_get('/tasks?status=ready')\nif not ready_tasks or not isinstance(ready_tasks, list):\n    print('stale-ready: no ready tasks or fetch failed')\n    sys.exit(0)\n\nnow = datetime.now(timezone.utc)\nalerted = 0\n\nfor task in ready_tasks:\n    tid = task.get('id')\n    title = task.get('title', '')\n    updated_at = task.get('updated_at')\n    stale_count = task.get('stale_count', 0)\n\n    if not updated_at:\n        continue\n\n    try:\n        updated_dt = datetime.fromisoformat(updated_at.replace('Z', '+00:00'))\n    except Exception:\n        continue\n\n    age_seconds = (now - updated_dt).total_seconds()\n    if age_seconds <= STALE_THRESHOLD_SECONDS:\n        continue\n\n    # Already alerted this cycle if stale_count > 0\n    if stale_count > 0:\n        print(f'[SKIP] Task {tid} ({title[:50]}): already alerted (stale_count={stale_count})')\n        continue\n\n    # Compute human-readable age\n    total_minutes = int(age_seconds // 60)\n    hours = total_minutes // 60\n    minutes = total_minutes % 60\n\n    # Increment stale_count to prevent duplicate alerts\n    api_patch(f'/tasks/{tid}', {'stale_count': stale_count + 1})\n\n    # Post alert to Discord #tasks channel\n    alert_msg = f'\\u26a0\\ufe0f STALE READY: {title} (ID: {tid}) has been in ready status for {hours}h {minutes}m \\u2014 task_executor may have missed it'\n    subprocess.run(['send_discord_channel', 'tasks', alert_msg], capture_output=True, text=True)\n    print(f'[STALE-READY] Task {tid} ({title[:50]}): {hours}h {minutes}m in ready — alerted Discord #tasks')\n    alerted += 1\n\nif alerted == 0:\n    print('stale-ready: no tasks stuck in ready for >4h')\nelse:\n    print(f'stale-ready: {alerted} stale-ready alert(s) sent to Discord #tasks')\nPYEOF\n\n(6) STALE IN-PROGRESS CHECK — find in_progress tasks with no recent activity that may be stuck:\n\npython3 << 'SIPEOF'\nimport json, subprocess, sys\nfrom datetime import datetime, timezone\n\napi_key = '462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\nbase = 'http://localhost:3333/api'\nSTALE_IP_THRESHOLD_SECONDS = 3 * 24 * 3600  # 3 days\nACTIVITY_LOOKBACK_SECONDS = 3 * 24 * 3600   # 3 days\nHEARTBEAT_RECENT_SECONDS = 2 * 3600         # 2 hours\n\ndef api_get(path):\n    r = subprocess.run(['curl', '-s', f'{base}{path}', '-H', f'x-api-key: {api_key}'], capture_output=True, text=True)\n    try: return json.loads(r.stdout)\n    except: return None\n\ndef api_patch(path, data):\n    r = subprocess.run(['curl', '-s', '-X', 'PATCH', f'{base}{path}', '-H', f'x-api-key: {api_key}', '-H', 'Content-Type: application/json', '-d', json.dumps(data)], capture_output=True, text=True)\n    return r.returncode == 0\n\nip_tasks = api_get('/tasks?status=in_progress')\nif not ip_tasks or not isinstance(ip_tasks, list):\n    print('stale-in-progress: no in_progress tasks or fetch failed')\n    sys.exit(0)\n\nactivity = api_get('/activity?limit=200') or []\nif not isinstance(activity, list): activity = []\n\nnow = datetime.now(timezone.utc)\nalerted = 0\n\nfor task in ip_tasks:\n    tid = task.get('id')\n    title = task.get('title', '')\n    updated_at = task.get('updated_at')\n    heartbeat_at = task.get('heartbeat_at')\n    stale_count = task.get('stale_count', 0)\n\n    # Skip if heartbeat is recent (task is actively running)\n    if heartbeat_at:\n        try:\n            hb_dt = datetime.fromisoformat(heartbeat_at.replace('Z', '+00:00'))\n            if (now - hb_dt).total_seconds() <= HEARTBEAT_RECENT_SECONDS:\n                print(f'[SKIP] Task {tid} ({title[:50]}): heartbeat recent — actively running')\n                continue\n        except Exception:\n            pass\n\n    if not updated_at:\n        continue\n\n    try:\n        updated_dt = datetime.fromisoformat(updated_at.replace('Z', '+00:00'))\n    except Exception:\n        continue\n\n    age_seconds = (now - updated_dt).total_seconds()\n    if age_seconds <= STALE_IP_THRESHOLD_SECONDS:\n        continue\n\n    # Check activity log for any entry mentioning this task ID in the last 3 days\n    recent_activity = [\n        a for a in activity\n        if tid in (a.get('summary') or '') and\n        a.get('created_at') and\n        (now - datetime.fromisoformat(a['created_at'].replace('Z', '+00:00'))).total_seconds() <= ACTIVITY_LOOKBACK_SECONDS\n    ]\n\n    if recent_activity:\n        print(f'[SKIP] Task {tid} ({title[:50]}): recent activity log entry found — not stale')\n        continue\n\n    # Compute human-readable age\n    total_seconds = int(age_seconds)\n    days = total_seconds // 86400\n    hours = (total_seconds % 86400) // 3600\n\n    # Increment stale_count to prevent duplicate alerts\n    api_patch(f'/tasks/{tid}', {'stale_count': stale_count + 1})\n\n    # Post alert to Discord #tasks channel\n    alert_msg = f'\\u26a0\\ufe0f STALE IN-PROGRESS: {title} (ID: {tid}) has been in_progress for {days}d {hours}h with no recent activity log \\u2014 may be stuck or forgotten'\n    subprocess.run(['send_discord_channel', 'tasks', alert_msg], capture_output=True, text=True)\n    print(f'[STALE-IN-PROGRESS] Task {tid} ({title[:50]}): {days}d {hours}h in_progress with no activity — alerted Discord #tasks')\n    alerted += 1\n\nif alerted == 0:\n    print('stale-in-progress: no tasks stuck in in_progress for >3d without activity')\nelse:\n    print(f'stale-in-progress: {alerted} stale-in-progress alert(s) sent to Discord #tasks')\nSIPEOF\n\nActual implementation: use Bash to run the curl health check, then fetch in_progress tasks, then use python3 inline to process the expired ones as described above. No LLM calls — pure curl + python3. Log what you do. Return a brief summary of how many tasks were reset, how many hit max retries, how many had no expired leases, how many stale-ready alerts were sent, and how many stale-in-progress alerts were sent.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_watchdog",
    "enabled": false,
    "type": "script",
    "script_command": "python3 /home/tchism/git/hollow/bin/watchdog"
  },
  {
    "name": "agent_upgrades",
    "schedule": "0 8 * * *",
    "tz": "America/Chicago",
    "prompt": "Run the daily agent upgrades scrape. Find the latest AI agent improvements, tooling updates, and research that could improve Hollow.\n\nSteps:\n1. Use xsearch to search X/Twitter for recent posts (last 24h) on: \"Claude Code agents\", \"multi-agent systems\", \"AI agent architecture\", \"claude claude-code\", \"LLM agent tooling\". Capture the top finds.\n2. Use WebSearch to search: site:reddit.com/r/ClaudeAI OR site:reddit.com/r/LocalLLaMA new agent posts from last 24h. Also search GitHub trending repositories for \"AI agents\" or \"LLM agent\".\n3. Compile the top 5-8 finds — things that could directly improve Hollow: new patterns, tools, prompting techniques, agent architecture ideas, SDK updates. Skip noise and hype.\n4. For each find: one sentence on what it is, one sentence on why it matters for Hollow specifically.\n5. Post the compiled list to #agent-upgrades using: send_discord_channel agent-upgrades \"[formatted finds]\"\n6. ARCHITECTURE GAP CHECK — After finding upgrade candidates, scan the current open task list for any tasks that introduce new capabilities without a corresponding agents.md update. Steps:\n  (a) Fetch open tasks: `mc tasks list --status=ready,backlog,in_progress,blocked`\n  (b) Read ~/git/hollow/agent-memory/tarn/agents.md to understand the current known capabilities, agent roles, and inter-agent paths.\n  (c) For each task in the open list, check whether it introduces any of the following WITHOUT a corresponding update to agents.md: new agent roles, new inter-agent communication paths (e.g. a new hail route or agent-to-agent delegation pattern), new persistent storage (new databases, new data dirs, new SQLite files), or new external integrations (new APIs, new third-party services, new webhooks).\n  (d) For each gap found: create a backlog task via `~/git/hollow/bin/mc tasks create \"Architecture review needed: [capability]\" --description=\"Task [id] introduces [capability] without agents.md update. Forge: review and update architecture docs to reflect this change.\" --tags=\"architecture-review\" --priority=medium`. Log each creation with the originating task ID.\n  (e) If no gaps found, log: 'architecture-gap-check: no gaps detected this cycle'\n  This step does NOT block the rest of the cron — run it after upgrade candidates are posted.\n8. Return the finds as your final response.\n\nDo NOT call send_msg or send_discord (those go to #tarn). Only use send_discord_channel.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "agent-upgrades",
    "chat_id": "cron_agent_upgrades",
    "enabled": false
  },
  {
    "name": "agent_upgrades_triage",
    "schedule": "0 9 * * *",
    "tz": "America/Chicago",
    "prompt": "Run the agent-upgrades triage. NOTE: handle this yourself without consulting other agents. Review recent posts in #agent-upgrades and decide which ones are worth a spike ticket.\n\nSteps:\n1. Fetch recent messages from #agent-upgrades using: get_discord_channel_history agent-upgrades 50\n   Filter to messages posted since yesterday at 8am CT. Skip any message with a 'TRIAGED' marker.\n   If there are no new candidate items, log: 'agent-upgrades-triage: no new items this cycle' and exit.\n\n2. For each candidate item, evaluate it yourself on four criteria:\n   a. BUILDABILITY: Is this buildable and reasonably scoped for a single spike (1-3 days)?\n   b. RISK/DUPLICATION: Any risks, scope creep, or overlap with existing tasks?\n   c. STRATEGIC FIT: Does this fit where Hollow is going? Distraction or genuine unlock?\n   d. SIGNAL QUALITY: Is the underlying claim sound, or is this hype?\n\n3. Based on your four-criteria assessment, decide APPROVE or REJECT.\n\n4. If APPROVED: create a spike task and post TRIAGED result to #agent-upgrades (same format as before).\n\n5. If REJECTED: log internally or post interesting-reject if warranted.\n\n6. Return a triage summary as your final response.\n\nDo NOT call send_msg or send_discord (those go to #tarn). Only use send_discord_channel for #agent-upgrades posts.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "agent-upgrades",
    "chat_id": "cron_agent_upgrades_triage",
    "enabled": false
  },
  {
    "name": "maintenance_restart",
    "schedule": "0 3 * * *",
    "tz": "America/Chicago",
    "prompt": "Run the maintenance restart check.\n\nSteps:\n\n(1) Read ~/git/hollow/agent-memory/tarn/pending-restart.md. If the file has no pending items (empty or only contains the header line with no bullet points), print 'maintenance_restart: no pending items — nothing to do' and exit.\n\n(2) Check for tasks currently in_progress:\n  IN_PROGRESS=$(curl -s 'http://localhost:3333/api/tasks?status=in_progress' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' | python3 -c \"import json,sys; tasks=json.load(sys.stdin); print(len(tasks) if isinstance(tasks, list) else 0)\")\n  If IN_PROGRESS is greater than 0, print 'maintenance_restart: deferred, tasks in progress' and exit.\n\n(3) If clear: run the restart with maintenance flag:\n  TARN_RESTART_REASON=maintenance bash ~/git/hollow/bin/restart-tarn\n  Then clear the pending items from pending-restart.md — write back only the header line:\n  echo '## Pending restart items' > ~/git/hollow/agent-memory/tarn/pending-restart.md\n\nFull implementation:\n\nPENDING=$(cat ~/git/hollow/agent-memory/tarn/pending-restart.md 2>/dev/null || echo '')\nif ! echo \"$PENDING\" | grep -q '^- '; then\n  echo 'maintenance_restart: no pending items — nothing to do'\n  exit 0\nfi\n\nIN_PROGRESS=$(curl -s 'http://localhost:3333/api/tasks?status=in_progress' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' | python3 -c \"import json,sys; tasks=json.load(sys.stdin); print(len(tasks) if isinstance(tasks, list) else 0)\" 2>/dev/null || echo '0')\nif [ \"$IN_PROGRESS\" -gt 0 ]; then\n  echo \"maintenance_restart: deferred, $IN_PROGRESS task(s) in progress\"\n  exit 0\nfi\n\necho 'maintenance_restart: conditions met — restarting Tarn'\necho '## Pending restart items' > ~/git/hollow/agent-memory/tarn/pending-restart.md\nTARN_RESTART_REASON=maintenance bash ~/git/hollow/bin/restart-tarn",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_maintenance_restart",
    "enabled": false,
    "type": "script",
    "script_command": "bash /home/tchism/git/hollow/bin/maintenance-restart 2>&1 || true"
  },
  {
    "name": "condition_resolver",
    "schedule": "*/30 * * * *",
    "tz": "America/Chicago",
    "prompt": "Run the Condition-Blocked Auto-Resolver. No LLM calls — pure curl + python3.\n\nGOAL: When a blocked or condition_blocked task lists another MC task as its dependency (in blocked_reason), and that dependency is now done, automatically unblock the waiting task.\n\n(1) HEALTH CHECK — abort if MC is unhealthy:\n  STATUS=$(curl -s http://localhost:3333/api/health | python3 -c \"import json,sys; d=json.load(sys.stdin); print(d.get('status','unknown'))\" 2>/dev/null)\n  If STATUS is not 'ok' or 'degraded', print 'MC unhealthy — condition_resolver aborting' and exit.\n\n(2) FETCH ALL BLOCKED AND CONDITION_BLOCKED TASKS (two separate API calls):\n  BLOCKED=$(curl -s 'http://localhost:3333/api/tasks?status=blocked' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22')\n  CONDITION_BLOCKED=$(curl -s 'http://localhost:3333/api/tasks?status=condition_blocked' -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22')\n\n(3) FOR EACH TASK IN BOTH LISTS, CHECK FOR DEPENDENCY TASK IDs IN blocked_reason. Use python3 to:\n  - Parse both JSON lists and merge them, tracking each task's original status\n  - For each task, scan blocked_reason for patterns that look like MC task IDs (alphanumeric strings with hyphens/underscores, 15+ characters, matching pattern: [A-Za-z0-9_-]{15,})\n  - For each candidate task ID found, fetch it: curl -s http://localhost:3333/api/tasks/[dep_id] -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\n  - If the dependency task status is 'done', PATCH the waiting task to ready and clear blocked_reason:\n    - For both blocked and condition_blocked tasks: PATCH to status=ready\n    curl -s -X PATCH http://localhost:3333/api/tasks/[task_id] -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -H 'Content-Type: application/json' -d '{\"status\": \"ready\", \"blocked_reason\": null}'\n  - Log each unblocking:\n    curl -s -X POST http://localhost:3333/api/activity -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' -H 'Content-Type: application/json' -d '{\"action_type\": \"task.unblocked\", \"summary\": \"auto-unblocked: [task title] — dependency [dep task title] completed\"}'\n\nFull python3 implementation:\n\npython3 << 'PYEOF'\nimport json, re, subprocess, sys\nfrom datetime import datetime, timezone\n\napi_key = '462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22'\nbase = 'http://localhost:3333/api'\n\ndef api_get(path):\n    r = subprocess.run(['curl', '-s', f'{base}{path}', '-H', f'x-api-key: {api_key}'], capture_output=True, text=True)\n    try: return json.loads(r.stdout)\n    except: return None\n\ndef api_patch(path, data):\n    r = subprocess.run(['curl', '-s', '-X', 'PATCH', f'{base}{path}', '-H', f'x-api-key: {api_key}', '-H', 'Content-Type: application/json', '-d', json.dumps(data)], capture_output=True, text=True)\n    return r.returncode == 0\n\ndef api_post(path, data):\n    r = subprocess.run(['curl', '-s', '-X', 'POST', f'{base}{path}', '-H', f'x-api-key: {api_key}', '-H', 'Content-Type: application/json', '-d', json.dumps(data)], capture_output=True, text=True)\n    return r.returncode == 0\n\n# Fetch both blocked and condition_blocked tasks\nblocked = api_get('/tasks?status=blocked') or []\ncondition_blocked = api_get('/tasks?status=condition_blocked') or []\n\nif not isinstance(blocked, list): blocked = []\nif not isinstance(condition_blocked, list): condition_blocked = []\n\nall_tasks = blocked + condition_blocked\n\nif not all_tasks:\n    print('No blocked or condition_blocked tasks — exiting')\n    sys.exit(0)\n\n# Pattern: MC task IDs are alphanumeric + hyphens/underscores, 15-30 chars\nid_pattern = re.compile(r'[A-Za-z0-9_-]{15,30}')\nunblocked_count = 0\n\nfor task in all_tasks:\n    tid = task.get('id')\n    title = task.get('title', '')\n    original_status = task.get('status', 'blocked')\n    blocked_reason = task.get('blocked_reason')\n    task_tags = task.get('tags') or []\n\n    # NULL-REASON AUTO-UNBLOCK: if blocked_reason is null AND task has 'condition-blocked' tag,\n    # this was set by the automated resolver (not a human). Auto-unblock it.\n    # Discriminator: tasks with non-null blocked_reason still need dependency resolution below.\n    # Tasks with null blocked_reason but NO 'condition-blocked' tag were human-set — skip.\n    if blocked_reason is None and 'condition-blocked' in task_tags:\n        ok = api_patch(f'/tasks/{tid}', {'status': 'ready', 'blocked_reason': None})\n        if ok:\n            api_post('/activity', {'action_type': 'task.unblocked', 'summary': f'auto-unblocked (null-reason): {title[:60]} — condition-blocked tag with no blocked_reason, auto-resolved'})\n            print(f'[NULL-REASON UNBLOCK] {title[:60]}: condition-blocked with null reason, auto-unblocked to ready')\n            unblocked_count += 1\n        continue  # Move to next task regardless\n\n    if blocked_reason is None:\n        continue  # No reason and no condition-blocked tag — skip (human-set state, don't touch)\n\n    candidate_ids = id_pattern.findall(blocked_reason)\n    # Filter: skip the task's own ID and obvious non-IDs (words, paths)\n    candidate_ids = [c for c in candidate_ids if c != tid and '/' not in c and '.' not in c]\n    \n    for dep_id in candidate_ids:\n        dep = api_get(f'/tasks/{dep_id}')\n        if not dep or not isinstance(dep, dict): continue\n        if dep.get('status') != 'done': continue\n        \n        dep_title = dep.get('title', dep_id)\n        # Unblock the waiting task — always resolve to ready\n        ok = api_patch(f'/tasks/{tid}', {'status': 'ready', 'blocked_reason': None})\n        if ok:\n            api_post('/activity', {'action_type': 'task.unblocked', 'summary': f'auto-unblocked: {title[:60]} (was {original_status}) — dependency completed: {dep_title[:60]}'})\n            print(f'[UNBLOCKED] {title[:60]} (was {original_status}) — dependency done: {dep_title[:40]}')\n            unblocked_count += 1\n            break  # Once unblocked, move to next task\n\nif unblocked_count == 0:\n    print('condition_resolver: no condition-blocked tasks resolved this cycle')\nelse:\n    print(f'condition_resolver: {unblocked_count} task(s) auto-unblocked')\nPYEOF",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_condition_resolver",
    "enabled": false,
    "type": "script",
    "script_command": "python3 /home/tchism/git/hollow/bin/condition-resolver 2>&1 || true"
  },
  {
    "name": "dv_discovery",
    "schedule": "0 2 * * 0",
    "tz": "America/Chicago",
    "prompt": "Run the Daniel Vitalis podcast discovery pipeline: cd /home/tchism/rewild_podcast_clips && python dv_discovery.py 2>&1. Log results to agent-activity. If new episodes were found and ingested, post summary to Discord #tasks channel.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "tasks",
    "chat_id": "cron_dv_discovery",
    "enabled": false
  },
  {
    "name": "stale_project_check",
    "schedule": "0 9 * * 1",
    "tz": "America/Chicago",
    "prompt": "Run stale project detection. Sweep ~/git/hollow/projects/ and flag any project whose log.md has been silent for 7+ days.\n\nSteps:\n\n1. List all subdirectories in ~/git/hollow/projects/ — each is a project.\n\n2. For each project, read its log.md and check the most recent dated entry heading (format: ## YYYY-MM-DD). Parse the date and compute age in days from today.\n\n3. If age >= 7 days (or log.md has no dated entries at all), flag it as stale.\n\n4. If any stale projects found: post a report to #tasks:\n   send_discord_channel tasks \"⚠️ STALE PROJECTS (silent 7+ days):\\n[for each stale: - projects/<name>/log.md — last entry: YYYY-MM-DD (N days ago)]\\n\\nReview and either close, restart, or archive each.\"\n\n5. If no stale projects: log to activity only — mc activity log 'stale_project_check: all projects active'\n\nFull bash/python3 implementation:\n\npython3 << 'PYEOF'\nimport os\nimport re\nimport subprocess\nfrom datetime import datetime, timezone, timedelta\nfrom pathlib import Path\n\nprojects_dir = Path(os.path.expanduser('~/git/hollow/projects'))\ntoday = datetime.now(tz=timezone.utc).date()\nstale = []\n\nfor project in sorted(projects_dir.iterdir()):\n    if not project.is_dir():\n        continue\n    log_path = project / 'log.md'\n    if not log_path.exists():\n        stale.append((project.name, None, 'no log.md'))\n        continue\n    content = log_path.read_text()\n    dates = re.findall(r'^## (\\d{4}-\\d{2}-\\d{2})', content, re.MULTILINE)\n    if not dates:\n        stale.append((project.name, None, 'no dated entries in log.md'))\n        continue\n    last_date = max(datetime.strptime(d, '%Y-%m-%d').date() for d in dates)\n    age = (today - last_date).days\n    if age >= 7:\n        stale.append((project.name, last_date.isoformat(), f'{age} days ago'))\n\nif not stale:\n    print('stale_project_check: all projects active')\nelse:\n    lines = ['⚠️ STALE PROJECTS (silent 7+ days):']\n    for name, last, note in stale:\n        if last:\n            lines.append(f'- projects/{name}/log.md — last entry: {last} ({note})')\n        else:\n            lines.append(f'- projects/{name}/log.md — {note}')\n    lines.append('')\n    lines.append('Review and either close, restart, or archive each.')\n    message = '\\n'.join(lines)\n    subprocess.run(['bash', '-c', f'~/git/hollow/bin/send_discord_channel tasks {repr(message)}'], shell=False)\n    print(message)\nPYEOF",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "tasks",
    "chat_id": "cron_stale_project_check",
    "enabled": false,
    "type": "script",
    "script_command": "python3 /home/tchism/git/hollow/bin/stale-project-check 2>&1 || true"
  },
  {
    "name": "corpus_health_check",
    "schedule": "0 10 * * 1",
    "tz": "America/Chicago",
    "prompt": "Run corpus health check. Execute: ~/git/hollow/bin/check-corpus-health --all\n\nThe script exits 0 if all corpora are healthy, 1 if any are degraded. It handles Telegram alerting internally when degraded.\n\nCapture exit code:\n  OUTPUT=$(~/git/hollow/bin/check-corpus-health --all 2>&1)\n  RC=$?\n  echo \"$OUTPUT\"\n\nIf RC is 0 (all healthy): log to activity log only — `mc activity log 'corpus_health_check: all corpora healthy'` — and return the script output as your final response. Do NOT send a Telegram or Discord notification.\n\nIf RC is 1 (degraded): the script has already sent a Telegram alert to Tyler. Log to activity: `mc activity log 'corpus_health_check: DEGRADED — Telegram alert sent'`. Return the script output as your final response.\n\nDo NOT call send_msg or send_discord (those go to #tarn).",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_corpus_health_check",
    "enabled": false,
    "type": "script",
    "script_command": "/home/tchism/git/hollow/bin/check-corpus-health --all 2>&1 || true"
  },
  {
    "name": "heartbeat",
    "schedule": "*/5 * * * *",
    "tz": "America/Chicago",
    "enabled": true,
    "model": "local:gemma4:e4b",
    "prompt": "You are running a system health check for the Hollow multi-agent system. Execute each check using bash commands only. Do NOT use the LLM for any check logic — only invoke it at the end to format a failure alert if needed.\n\nRun all 5 checks in sequence. Collect results. At the end, report pass/fail.\n\n---\n\nCHECK 1 — Agent liveness\n\nFor each agent, run a health check. The agents and their ports are:\n- tarn (coordinator): port 18800\n- tap: port 18792\n- canopy: port 18793\n- briar: port 18794\n- forge: port 18795\n- spring: port 18796\n- reed: port 18797\n- journal: port 18798\n- flux: port 18799\n\nFor each agent, run:\n```bash\nHTTP_CODE=$(curl -s -o /dev/null -w '%{http_code}' http://localhost:<PORT>/health --max-time 3 2>/dev/null)\nif [ \"$HTTP_CODE\" = \"200\" ]; then\n  echo \"check1 <AGENT>: PASS (HTTP 200)\"\nelse\n  echo \"check1 <AGENT>: FAIL (HTTP $HTTP_CODE or timeout)\"\nfi\n```\n\nDo this for all 9 agents (tarn/18800, tap/18792, canopy/18793, briar/18794, forge/18795, spring/18796, reed/18797, journal/18798, flux/18799). Collect any FAILs.\n\n---\n\nCHECK 2 — MC API health\n\n```bash\nMC_RESPONSE=$(curl -s 'http://localhost:3333/api/tasks?limit=1' \\\n  -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' \\\n  --max-time 5 2>/dev/null)\nif echo \"$MC_RESPONSE\" | python3 -c \"import sys, json; json.load(sys.stdin)\" 2>/dev/null; then\n  echo \"check2 mc_api: PASS (valid JSON)\"\nelse\n  echo \"check2 mc_api: FAIL (timeout, non-JSON, or HTTP error) — response: $MC_RESPONSE\"\nfi\n```\n\n---\n\nCHECK 3 — Task lease audit\n\n```bash\nIN_PROGRESS=$(curl -s 'http://localhost:3333/api/tasks?status=in_progress' \\\n  -H 'x-api-key: 462f220e3c3f15324825a86373da874f92302bf6fc646e3330731468cc959c22' \\\n  --max-time 5 2>/dev/null)\nORPHANED=$(echo \"$IN_PROGRESS\" | python3 -c \"\nimport sys, json, datetime\ntry:\n    tasks = json.load(sys.stdin)\n    orphaned = []\n    for t in tasks:\n        lease = t.get('lease_expires_at')\n        if lease:\n            ts = datetime.datetime.fromisoformat(lease.replace('Z', '+00:00'))\n            now = datetime.datetime.now(datetime.timezone.utc)\n            if ts < now:\n                orphaned.append(f\\\"id={t.get('id')} title={t.get('title','?')} expired={lease}\\\")\n    if orphaned:\n        print('ORPHANED:' + '|'.join(orphaned))\n    else:\n        print('NONE')\nexcept Exception as e:\n    print(f'ERROR:{e}')\n\" 2>/dev/null)\nif [ \"$ORPHANED\" = \"NONE\" ]; then\n  echo \"check3 lease_audit: PASS (no orphaned tasks)\"\nelse\n  echo \"check3 lease_audit: FAIL — $ORPHANED\"\nfi\n```\n\n---\n\nCHECK 4 — Cron schedule verification (STUB)\n\n```bash\necho \"check4: skipped — APScheduler does not persist last_run_time; implementation deferred to Phase 2\"\n```\n\nNote filed: Full cron schedule verification requires adding last_run tracking to the scheduler. APScheduler tracks next_run_time but not last_run_time.\n\n---\n\nCHECK 5 — Disk and memory baseline\n\n```bash\nDISK_PCT=$(df --output=pcent $HOME/git/hollow/data/ 2>/dev/null | tail -1 | tr -d ' %')\nMEM_PCT=$(free | awk '/Mem:/{printf \"%.0f\", $3/$2*100}')\nif [ \"$DISK_PCT\" -gt 85 ] 2>/dev/null; then\n  echo \"check5 disk: FAIL — ${DISK_PCT}% used (threshold: 85%)\"\nelse\n  echo \"check5 disk: PASS — ${DISK_PCT}% used\"\nfi\nif [ \"$MEM_PCT\" -gt 90 ] 2>/dev/null; then\n  echo \"check5 memory: FAIL — ${MEM_PCT}% used (threshold: 90%)\"\nelse\n  echo \"check5 memory: PASS — ${MEM_PCT}% used\"\nfi\n```\n\n---\n\nFINAL STEP — Report and alert\n\nAfter running all 5 checks, review the collected output lines:\n\n- If ALL checks passed (no FAIL lines in output from checks 1, 2, 3, 5):\n  ```bash\n  mc activity log 'heartbeat: all checks passed'\n  ```\n  Then exit. No LLM formatting needed.\n\n- If ANY check failed: Use your language model capability to format a structured Discord alert. The alert must include for each failure:\n  - Check name (e.g., \"check1 forge agent liveness\")\n  - Expected state (e.g., \"HTTP 200 from /health\")\n  - Actual state (e.g., \"timeout after 3s\")\n  - Suggested action (e.g., \"run: ./start.sh forge\" or \"check logs at data/forge/\")\n\n  Format the alert as plain text (no markdown headers, use newlines). Keep it under 1800 characters for Discord.\n\n  Then post it:\n  ```bash\n  send_discord_channel system-health \"<your formatted alert>\"\n  ```\n\n  Also log to MC:\n  ```bash\n  mc activity log 'heartbeat: N checks failed — alert posted to #system-health'\n  ```",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "system-health",
    "chat_id": "cron_heartbeat",
    "_note": "Uses local Gemma4 via Ollama — no Claude API burn. Set enabled:true after Tyler confirms #system-health Discord channel exists. Check 4 (cron schedule verification) is a stub — APScheduler does not persist last_run_time; full implementation deferred to Phase 2."
  },
  {
    "name": "wiki_compiler",
    "schedule": "0 4 * * *",
    "tz": "America/Chicago",
    "prompt": "Run the wiki compiler. This is a nightly maintenance job that promotes raw agent dumps into the structured wiki and lints all wiki pages.\n\nSTEP 1 — SCAN RAW DIRECTORY\nList all .md files in agent-memory/wiki/raw/:\n  RAW_FILES=$(ls ~/git/hollow/agent-memory/wiki/raw/*.md 2>/dev/null)\n  if [ -z \"$RAW_FILES\" ]; then echo 'wiki_compiler: no raw files to process'; RAW_COUNT=0; else RAW_COUNT=$(echo \"$RAW_FILES\" | wc -l); fi\n\nSTEP 2 — PROCESS EACH RAW FILE\nFor each .md file found in agent-memory/wiki/raw/:\n\n  a. Read the raw file content.\n\n  b. Determine the target wiki page: inspect the raw file for a header comment like `<!-- wiki-target: path/to/page.md -->`. If present, use that path (relative to agent-memory/wiki/). If absent, derive a target from the filename (e.g., raw/tyler-voice-notes.md → tyler-voice.md, raw/hollow-arch-update.md → projects/hollow.md). If no mapping can be determined, create a new page at agent-memory/wiki/<basename-without-raw-prefix>.md.\n\n  c. If the target page EXISTS:\n     - Read the existing page.\n     - Append new, non-duplicate information from the raw file to the bottom of the existing page.\n     - Each appended block must include a source tag: `[source: raw/<filename>, compiled: YYYY-MM-DD]` where YYYY-MM-DD is today's date.\n     - DO NOT remove, overwrite, or restructure existing content. Additive only.\n     - Write the updated file back.\n\n  d. If the target page DOES NOT EXIST:\n     - Create a new wiki page at the target path.\n     - Format: start with a `# Title` heading, then structured sections with sourced claims.\n     - Keep the page under 2000 tokens (~1500 words). If the raw content is too large, summarize — preserve the key facts and source attributions.\n     - Tag every factual claim with `[source: raw/<filename>, compiled: YYYY-MM-DD]`.\n     - Add a retrieval hints block at the bottom:\n       ```\n       ## Retrieval Hints\n       [corpus: wiki, topics: <comma-separated key topics from the content>]\n       ```\n     - Write the new file.\n\n  e. After processing: move the raw file to agent-memory/wiki/raw/processed/ (create the directory if needed) to prevent re-processing on the next run. Name it <original-name>-processed-YYYYMMDD.md.\n\nTrack counts: RAW_PROCESSED, PAGES_UPDATED (existing page appended), PAGES_CREATED (new page created).\n\nSTEP 3 — UPDATE MEMORY.MD INDEX\nIf any new wiki pages were created (PAGES_CREATED > 0):\n  - Read agent-memory/wiki/MEMORY.md.\n  - Add each new page to the appropriate section (Entity Pages or Project Pages based on path).\n  - Use format: `- <filename> — <one-line description from the page's title/intro>`\n  - Write the updated MEMORY.md.\n\nIf no new pages were created, skip this step.\n\nSTEP 4 — LINT PASS\nRun a lint pass on ALL .md files in agent-memory/wiki/ (excluding raw/ and raw/processed/).\n\nFor each wiki page, check:\n\n  LINT-1 UNSOURCED CLAIMS: Scan for sentences containing factual assertions (dates, names, numbers, specific claims) that do not have a `[source: ...]` or `[corpus: ...]` tag on the same line or in the same paragraph. Flag as: `LINT-1 [filename] line ~N: unsourced claim — \"<excerpt>\"`\n\n  LINT-2 CONTRADICTIONS: Compare key facts across pages. Look for the same entity (person, project, date) described differently in two different pages. Flag as: `LINT-2 CONTRADICTION: <page-a> says X, <page-b> says Y — topic: <topic>`. Do NOT auto-fix. Log only.\n\n  LINT-3 STALE DATES: Find any date reference (YYYY-MM-DD or month/year) in a page that is more than 30 days old AND the page has no `[refreshed: YYYY-MM-DD]` marker anywhere in it. Flag as: `LINT-3 [filename]: date <DATE> is stale (>30 days), no refresh marker found`\n\nWrite all lint findings to agent-memory/wiki/lint-report.md (overwrite on each run). Format:\n```\n# Wiki Lint Report\nGenerated: YYYY-MM-DD\nPages scanned: N\n\n## Issues Found (N total)\n\n### LINT-1: Unsourced Claims\n...\n\n### LINT-2: Contradictions\n...\n\n### LINT-3: Stale Dates\n...\n\n## Summary\nLINT-1: N issues | LINT-2: N contradictions | LINT-3: N stale dates\n```\n\nIf no issues found, write: `# Wiki Lint Report\\nGenerated: YYYY-MM-DD\\nPages scanned: N\\n\\nNo issues found. All pages pass lint.`\n\nCritical: if LINT-2 (contradictions) were found, also write them to agent-memory/wiki/contradictions-alert.md (append, not overwrite) so they persist across runs for Tyler review.\n\nSTEP 5 — LOG TO MC\nCount total lint issues: LINT_TOTAL = LINT1_COUNT + LINT2_COUNT + LINT3_COUNT\n\nRun:\n  mc activity log \"wiki_compiler: processed $RAW_COUNT raw files, updated $PAGES_UPDATED pages, created $PAGES_CREATED pages, lint: $LINT_TOTAL issues found\"\n\nReturn your full execution summary as the final response. Include: raw files processed, pages updated/created, lint issue counts by type, and path to lint-report.md.",
    "model": "claude-sonnet-4-6",
    "is_main_session": false,
    "notify_telegram": false,
    "discord_channel_name": "",
    "chat_id": "cron_wiki_compiler",
    "enabled": true
  }
]
